/**
 * curriculum/ccna2/videos.ts — one verified video per CCNA 2 lesson, keyed by lesson id.
 *
 * STUB created by the lead before W6; the W6 course-videos builder fills it under the same verification rule as
 * `curriculum/ccna1/videos.ts` (oEmbed 200 with the recorded title and channel, playable in an embed, no sponsor read,
 * no vendor in the title or channel; ARCHITECTURE-P2 §11.3). A lesson with no entry runs theory-only.
 */
import type { LessonVideoMap } from '../../contracts/curriculum.js';

export const CCNA2_VIDEOS: LessonVideoMap = {};
