/**
 * curriculum/ccna2/theory-a.ts — plain-language theory for the first part of CCNA 2 (lessons 01-11: switches, VLANs, routing between VLANs), keyed by the lesson ids of
 * `ccna2/lessons.ts`. `theory-b.ts` carries 12-21 and `theory-c.ts` 22-24 and 29-34; `theory.ts` merges the three.
 *
 * STUB created by the lead before W6 so each course builder owns its file from the start; the W6 course builder for
 * lessons 01-11: switches, VLANs, routing between VLANs fills it (ARCHITECTURE-P2 §7 W6 course, §11.1, §11.3). Same markdown subset, five sections and rules as
 * `curriculum/ccna1/theory-a.ts`.
 */
import type { LessonTheoryMap } from '../../contracts/curriculum.js';

export const THEORY_A: LessonTheoryMap = {};
