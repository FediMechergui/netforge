/**
 * curriculum/ccna2/theory-c.ts — plain-language theory for the third part of CCNA 2 (lessons 22-24 and 29-34: access-layer security, static routing, translation and fault finding), keyed by the lesson ids of
 * `ccna2/lessons.ts`. `theory-a.ts` carries 01-11 and `theory-b.ts` 12-21; `theory.ts` merges the three. The wireless lessons 25-28 arrive in W7.
 *
 * STUB created by the lead before W6 so each course builder owns its file from the start; the W6 course builder for
 * lessons 22-24 and 29-34: access-layer security, static routing, translation and fault finding fills it (ARCHITECTURE-P2 §7 W6 course, §11.1, §11.3). Same markdown subset, five sections and rules as
 * `curriculum/ccna1/theory-a.ts`.
 */
import type { LessonTheoryMap } from '../../contracts/curriculum.js';

export const THEORY_C: LessonTheoryMap = {};
